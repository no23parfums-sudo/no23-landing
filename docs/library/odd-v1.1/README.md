# NO.23 ODD v1.1 — Cursor package

Place this entire folder inside the repository at:

`docs/library/odd-v1.1/`

The canonical human-editable source remains the original Excel workbook.
For implementation, Cursor should read `NO23_ODD_v1.1.md` and the CSV files.

Important:
- The workbook currently defines the Perfume dictionary, controlled catalogs, the initial Bleu de Chanel EDP pilot template, sources structure, and multimedia structure.
- It does not yet fully define every field for every related entity.
- Cursor must not invent missing related-entity schemas. It should implement only what is explicitly defined or stop and ask.
